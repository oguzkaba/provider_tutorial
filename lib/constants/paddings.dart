import 'package:flutter/widgets.dart';

const lowVPadding = Padding(padding: EdgeInsets.symmetric(vertical: 10));
const midVPadding = Padding(padding: EdgeInsets.symmetric(vertical: 20));
const highVPadding = Padding(padding: EdgeInsets.symmetric(vertical: 30));

const lowHPadding = Padding(padding: EdgeInsets.symmetric(horizontal: 5));
const midHPadding = Padding(padding: EdgeInsets.symmetric(horizontal: 20));
const highHPadding = Padding(padding: EdgeInsets.symmetric(horizontal: 30));
